# AI Life - Environment Configuration
# Copy this file to .env and customize for your deployment

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# Default admin user password (CHANGE THIS FOR SECURITY)
# Must be at least 6 characters long
ADMIN_PASSWORD=admin123

# JWT secret key (CHANGE THIS FOR SECURITY)
# Must be at least 32 characters long for security
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-secret-key-in-production-min-32-chars

# Optional: Custom admin username (defaults to 'admin')
# ADMIN_USERNAME=admin

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Application port (default: 8000)
PORT=8000

# Environment mode (development, production)
NODE_ENV=production

# Application URL (for generating links, if behind reverse proxy)
# APP_URL=http://localhost:8000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# SQLite database file path (relative to /app in container)
# DATABASE_PATH=data/ailife.db

# Journal directory path (relative to /app in container)
# JOURNAL_PATH=journal

# ===========================================
# DOCKER & DEPLOYMENT
# ===========================================

# Data persistence directory (for Docker volumes)
# DATA_DIR=/app/data

# Journal directory (for Docker volumes)
# JOURNAL_DIR=/app/journal

# ===========================================
# OPTIONAL FEATURES
# ===========================================

# Enable debug logging (true/false)
# DEBUG=false

# Session timeout in days (default: 7)
# SESSION_TIMEOUT_DAYS=7

# Maximum file upload size in MB
# MAX_UPLOAD_SIZE=10

# ===========================================
# SECURITY HEADERS
# ===========================================

# Enable HTTPS redirects (if behind reverse proxy with SSL)
# HTTPS_REDIRECT=false

# CORS origins (comma-separated, or * for any)
# CORS_ORIGINS=*
